enum ErrorForNotification {
  NOTIFICATION_SEND_ERROR = 'The notification could not be sent'
}

enum MessagesForNotification {
  NOTIFICATION_SEND_SUCCESS = 'The notification was sent successfully'
}

export { ErrorForNotification as EFN, MessagesForNotification as MFN }
